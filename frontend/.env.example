# Example frontend environment variables for Vite
# Vite requires env vars exposed to client to start with VITE_

# URL of your backend API (include /api if that's your base path)
VITE_API_URL=https://your-backend.example.com/api

# Any other public feature flags or public-only keys (optional)
# VITE_PUBLIC_FEATURE_FLAG=true
